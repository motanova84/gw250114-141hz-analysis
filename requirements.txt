# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
astropy>=5.0
h5py>=3.7.0
jupyter>=1.0.0
python-dotenv>=1.0.0
mpmath>=1.3.0
sympy>=1.12
pandas>=1.3.0
pyyaml>=6.0
qcodes>=0.37.0
flask>=2.0.0

# Performance optimization dependencies (optional)
joblib>=1.1.0  # Parallel processing
numexpr>=2.8.0  # Fast numerical expressions

# Optional: GPU acceleration (requires CUDA)
# cupy-cuda12x>=12.0.0  # Uncomment for CUDA 12.x support
# cupy-cuda11x>=11.0.0  # Uncomment for CUDA 11.x support

# Optional: Distributed computing
# dask[complete]>=2023.1.0  # Uncomment for Dask support
# distributed>=2023.1.0

# Optional: Advanced compression
# zarr>=2.13.0  # Uncomment for Zarr support
# pyarrow>=10.0.0  # Uncomment for Parquet support

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=22.0.0
flake8>=4.0.0
mypy>=0.990

# Optional: LLM integration
openai>=1.0.0
anthropic>=0.8.0

# QC-LLM Core Dependencies (for coherence computation)
transformers>=4.48.0  # Security: CVE fixes for deserialization
torch>=2.6.0  # Security: CVE fixes for RCE and buffer overflow
sentencepiece>=0.1.99  # For BERT tokenization
